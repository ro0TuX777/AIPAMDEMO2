# AIPAM V2 Backend — Python dependencies
# Install with: pip install -r requirements.txt

# --- Core API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9

# --- Database ---
sqlalchemy>=2.0
alembic>=1.13

# --- Task queue ---
celery>=5.3
redis>=5.0

# --- HTTP client ---
httpx>=0.27
requests>=2.31

# --- Network analysis ---
scapy>=2.5

# --- Templates / rendering ---
jinja2>=3.1

# --- Numeric ---
numpy>=1.26

# --- Docker SDK (sensor containers) ---
docker>=7.0

# --- RAG / Vector Store ---
# Pinned: chromadb 1.5.x ("RustBindingsAPI has no attribute 'bindings'" /
# "Could not connect to tenant default_tenant") cannot open stores that 1.4.x
# created, which silently breaks ALL vector ops (KB indexing, pipeline
# auto-indexing, chat RAG). 1.4.0 reads the existing store and is what the test
# suite runs against. Do not loosen without re-testing against a real store.
chromadb==1.4.0

# --- Document parsing (KB binary uploads) ---
pdfplumber>=0.10
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6.23
# OCR fallback for image-only / scanned PDFs. Needs the tesseract-ocr and
# poppler-utils system packages (installed in deploy/Dockerfile.app).
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0

# --- Testing ---
pytest>=8.0

# --- Linting ---
ruff>=0.4

# --- V1 compatibility (training, embeddings) ---
sqlmodel
lancedb>=0.6.0
pyarrow>=14.0
sentence-transformers>=2.2.0

# --- Binary / YARA analysis (backend.app.binalysis) ---
# Provides the `yara` module; without it binary analysis still hashes and
# measures entropy but reports yara_available=false and matches nothing.
yara-python>=4.3.0

# BlueScrub vendored binary analysis (PE/ELF/Mach-O parsing, disassembly).
# Every import is guarded by an *_AVAILABLE flag upstream, so a missing package
# degrades to "no findings" rather than an error — indistinguishable from a
# clean binary. Pin them here so that degradation is a deployment choice
# rather than an accident. See backend/app/bluescrub/VENDOR.md.
pefile>=2023.2.7
pyelftools>=0.31
capstone>=5.0.0
lief>=0.14.0
