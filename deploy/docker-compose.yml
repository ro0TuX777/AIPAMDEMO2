# ============================================
# DEPRECATED — Use the root docker-compose.yml
# ============================================
#
# This file is kept as a redirect stub for backward compatibility.
# The root docker-compose.yml is now the single source of truth.
#
# Usage (from the repo root):
#   docker compose up -d
#   docker compose --profile arkime up -d
#
# See the root docker-compose.yml for full documentation.

